{
  "name": "w3x-to-toml",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "build": "esbuild renderer.js --platform=node --target=esnext --format=cjs --bundle --outfile=dist/renderer.js && electron --inspect=5858 --enable-logging .",
    "start": "electron ."
  },
  "author": "nazarpunk <nazarpunk@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "warodel": "^1.0.9"
  },
  "devDependencies": {
    "esbuild": "0.18.10",
    "electron": "^25.2.0",
    "electron-packager": "^17.1.1"
  }
}
